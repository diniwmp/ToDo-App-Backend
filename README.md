# ToDo-App-Backend
A lightweight Java backend (Servlets + Hibernate ORM + MySQL) powering a Todo app, providing REST endpoints for user authentication and full CRUD todo management with JSON responses via Gson.
